{
  "name": "luskahz-profile-engine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Perfil dev com métricas públicas, carrossel SVG e arcade interativo.",
  "scripts": {
    "generate": "node src/generate-profile.mjs",
    "test": "node --test",
    "check": "node --check src/generate-profile.mjs && node --check site/app.js && node --check site/game-data.js && node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
